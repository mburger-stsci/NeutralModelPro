pro model_common_blocks

;; Defines the common blocks used in the model
common constants, SystemConsts, DipoleConsts, stuff, positions
common ratecoefs, kappa
common plasma, plasma, plasmahot
common sticking, sticking_map, emit

end
